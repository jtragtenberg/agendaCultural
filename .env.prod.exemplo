# Copie este arquivo para .env.prod e preencha os valores reais
# Nunca commite o .env.prod

# Banco de dados
POSTGRES_DB=agenda_cultural
POSTGRES_USER=postgres
POSTGRES_PASSWORD=troque_por_senha_forte

# JWT — gere com: openssl rand -hex 32
JWT_SEGREDO=troque_por_segredo_forte

# URL da API acessível pelo navegador do usuário
# Use o IP ou domínio do servidor, porta 3000
VITE_API_URL=http://104.131.127.99:3000
